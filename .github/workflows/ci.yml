name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.13"]

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Lint
        run: ruff check postmind/

      - name: Format check
        run: ruff format --check postmind/ tests/

      - name: Security scan
        run: bandit -r postmind/ -ll -q

      - name: Test
        run: pytest tests/ -v --tb=short --cov=postmind --cov-report=term-missing
        env:
          # Tests use MockAIEngine — no real key needed
          ANTHROPIC_API_KEY: ""

      - name: Build check
        run: |
          pip install build twine
          python -m build
          twine check dist/*
